# Overview

OpenSciFlow Verified Capsules is a draft registry for execution-facing evidence packages.

A capsule is not a guarantee that a tool runs everywhere. It is a structured record of requirements, command templates, smoke tests, run records, verified environments, and known failures.

The first goal is correction: make missing fields, unsafe assumptions, and unverifiable claims easier to find.

# Run Records

No successful or failed run records are included yet.

When execution starts, write one JSON run record per attempt. Failed attempts are useful if they capture the command, return code, logs, environment, and known failure match.
